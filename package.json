{
  "name": "phenomap",
  "private": true,
  "version": "0.0.0",
  "description": "PhenoMap — Global UAP Intelligence Platform",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
